import { WritingObjectArchiveHeroRevealPreviewLazy } from '@/components/writings/WritingObjectArchiveHeroRevealPreviewLazy'
import { WritingObjectArchiveLeftPanelPreviewLazy } from '@/components/writings/WritingObjectArchiveLeftPanelPreviewLazy'

export const writingMeta = {
  id: 'object-and-archive-hero-opening',
  order: 1,
  title: 'Object & Archive: hero opening and left carousel',
  date: '2026-05-14',
  category: 'UI',
  excerpt:
    'Curtain hero plus a sixteen-layer left stack: GSAP tweens a scale ladder (2.7754 / 1 / 0.01) through all CDN plates, then every layer rests at the top zoom.',
  coverSrc: '/media/writings/object-and-archive-hero-reference.png',
  titleLines: ['Object & Archive,', 'hero + left carousel'],
  asideParagraphs: [
    'The live site loads the same assets from Shopify CDN; hotlinking here matches their URLs for width variants.',
    'Curtain color in the first preview is a stand-in gradient (`--writing-oa-hero-curtain`); swap the token to match brand QA without touching markup.',
  ],
}

This note walks **two** transitions from [Object & Archive](https://objectandarchive.com/).

## First beat: curtain + split

A **tinted full-bleed layer** lifts off the diptych—**Étude de forêt** on the left, **Monet detail** on the right—while the **wordmark stays on top** of both the panel and the photography, with a light gradient scrim so the SVG stays legible.

<WritingObjectArchiveHeroRevealPreviewLazy />

### Layer model (curtain)

1. **Split media** — two halves, `object-fit: cover`, full-bleed in the stage (`inset: 0`).
2. **Curtain** — same box as the stage, muted sage gradient (`--writing-oa-hero-curtain`); `transform: translateY(-100%)` when open.
3. **Wordmark overlay** — highest z-index, bottom-centered `oa-white.svg`, soft dark gradient scrim behind it for contrast on both the curtain and the paintings.

Replay runs the same sequence without reloading the page. Reduced-motion visitors skip the tween and see the revealed layout immediately.

## Second beat: left-column stack

The left half is a **single stack** of sixteen Shopify images (each slide’s background then inset, in order). **Next →** advances a **scale ladder** in one GSAP pass: layers already passed sit at **2.7754**, the current focal layer at **1**, and deeper layers at **0.01**—then the pattern shifts forward. **← Back** runs the same tween in reverse. The badge tracks depth (1–16); the final step holds every layer at **2.7754**. The right half stays a static Monet detail.

<WritingObjectArchiveLeftPanelPreviewLazy />
